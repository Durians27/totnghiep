@model VelvySkinWeb.Models.Category
@{
    ViewData["Title"] = "Sửa Danh Mục";
    Layout = "~/Views/Shared/_AdminLayout.cshtml"; // Giữ lại Topbar và Sidebar
}

<style>
    /* CSS Giao diện Edit của Như - Đã tinh gọn */
    .edit-cat-wrapper {
        --primary-mint: #2ec4b6; --dark-mint: #208b81; --light-mint: #f0fdfa;
        --bg-gray: #f8f9fa; --gold: #c5a059; --danger: #ff5e5e;
        font-family: 'Montserrat', sans-serif; color: #333;
    }
    
    .edit-cat-wrapper .page-header { margin-bottom: 30px; }
    .edit-cat-wrapper .page-title { font-family: 'Playfair Display', serif; font-size: 28px; color: var(--dark-mint); margin: 0; }
    .edit-cat-wrapper .page-subtitle { font-size: 13px; color: #aaa; margin-top: 5px; }

    .edit-cat-wrapper .form-card { background: white; border-radius: 30px; padding: 40px; box-shadow: 0 10px 40px rgba(0,0,0,0.02); border: 1px solid #f1f1f1; max-width: 850px; margin: 0 auto;}

    .edit-cat-wrapper .form-group { margin-bottom: 20px; }
    
    .edit-cat-wrapper label { display: block; font-size: 11px; font-weight: 700; color: #888; text-transform: uppercase; margin-bottom: 10px; letter-spacing: 0.5px; }
    
    .edit-cat-wrapper input, .edit-cat-wrapper textarea { width: 100%; padding: 14px 18px; border-radius: 15px; border: 1px solid #eee; background: #fcfcfc; font-family: inherit; font-size: 14px; color: #444; outline: none; transition: 0.3s; box-sizing: border-box;}
    .edit-cat-wrapper input:focus, .edit-cat-wrapper textarea:focus { border-color: var(--primary-mint); background: white; box-shadow: 0 0 0 4px var(--light-mint); }

    .edit-cat-wrapper .btn-group { display: flex; gap: 15px; margin-top: 40px; padding-top: 25px; border-top: 1px solid #f9f9f9; }
    .edit-cat-wrapper .btn { padding: 15px 30px; border-radius: 15px; font-weight: 700; font-size: 14px; cursor: pointer; border: none; transition: 0.3s; display: flex; align-items: center; gap: 10px; text-decoration: none;}
    .edit-cat-wrapper .btn-submit { background: var(--primary-mint); color: white; flex: 2; justify-content: center; box-shadow: 0 10px 20px rgba(46, 196, 182, 0.15); }
    .edit-cat-wrapper .btn-cancel { background: #f5f5f5; color: #888; flex: 1; justify-content: center; }
    .edit-cat-wrapper .btn-submit:hover { background: var(--dark-mint); transform: translateY(-2px); }
</style>

<div class="edit-cat-wrapper">
    <div style="max-width: 850px; margin: 0 auto;">
        <div style="font-size: 12px; color: #aaa; margin-bottom: 15px; font-weight: 600; text-transform: uppercase; letter-spacing: 1px;">
            Quản lý / Danh mục / <span style="color: var(--primary-mint);">Sửa thông tin</span>
        </div>

        <div class="page-header">
            <h1 class="page-title">Sửa Danh Mục: @Model.Name</h1>
            <p class="page-subtitle">Cập nhật thông tin phân loại sản phẩm</p>
        </div>

        <div class="form-card">
            <form asp-controller="Admin" asp-action="EditCategory" method="post">
                <input type="hidden" asp-for="Id" />
                <div class="form-group">
                    <label>Thuộc nhóm danh mục lớn <span style="color: var(--danger);">*</span></label>
                    <select asp-for="CategoryGroup" required>
                        <option value="">-- Chọn nhóm phân loại --</option>
                        <option value="Chăm sóc da">Chăm sóc da</option>
                        <option value="Chăm sóc tóc">Chăm sóc tóc</option>
                        <option value="Trang điểm">Trang điểm</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>Tên danh mục <span style="color: var(--danger);">*</span></label>
                    <input asp-for="Name" type="text" required>
                    <span asp-validation-for="Name" class="text-danger" style="font-size: 12px; margin-top: 5px; display: block;"></span>
                </div>

                <div class="form-group">
                    <label>Mô tả danh mục</label>
                    <textarea asp-for="Description" rows="4" placeholder="Viết vài dòng giới thiệu về danh mục này..."></textarea>
                </div>

                <div class="form-group" style="max-width: 50%;">
                    <label>Thứ tự hiển thị (Tùy chọn)</label>
                    <input type="number" name="DisplayOrder" value="1" min="1" title="Tính năng này sẽ được kích hoạt ở bản cập nhật DB sau">
                </div>

                <div class="btn-group">
                    <a asp-controller="Admin" asp-action="Categories" class="btn btn-cancel">Quay lại</a>
                    <button type="submit" class="btn btn-submit">
                        <i class="fa-solid fa-check"></i> Lưu thay đổi
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>